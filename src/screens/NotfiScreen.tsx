import React from 'react'
import { View, Text } from 'react-native'

interface Props {
    
}

const NotfiScreen = (props: Props) => {
    return (
        <View>
            <Text>Nhận thông báo</Text>
        </View>
    )
}

export default NotfiScreen
