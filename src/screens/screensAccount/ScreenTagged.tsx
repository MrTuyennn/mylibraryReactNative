import React from 'react';
import {View, Text} from 'react-native';

interface Props {}

const ScreenTagged = (props: Props) => {
  return (
    <View>
      <Text>Danh sách được gán thẻ</Text>
    </View>
  );
};

export default ScreenTagged;
